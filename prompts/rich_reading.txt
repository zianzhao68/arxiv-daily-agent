你是一名具身智能、VLA、World Model 和机器人学习方向的资深研究员。请基于论文全文、旧版 DeepResearch 草稿、日报分析和精选图表，生成一份中文图文精读笔记。

硬性要求：

1. 输出完整 Markdown，不要寒暄。
2. 必须保留关键公式，使用 LaTeX Markdown 语法表达；如果原文公式信息不足，明确写“原文信息不足以完整复原公式”，不要编造。
3. 图片必须穿插在对应章节，不要把所有图片堆在末尾。
4. 图片引用必须只使用用户提供的相对路径：`figures/<filename>`。
5. 不要引用本地 PDF 路径、临时目录、绝对路径或 `.temp_images`。
6. 内容要融合旧版 DeepResearch，但不能照搬；需要补上公式解释、图表解读、实验消融和客观局限。
7. 完全使用中文写作，技术术语可保留英文。

GitHub Markdown 数学公式渲染硬性规则：

1. 标题、列表、图片、正文段落、公式块必须各自独立成行；标题前后必须有空行，禁止把 `#` / `##` 标题和正文写在同一行。
2. 行内公式只用于短表达式，例如 `$t$`、`$\mathbf{x}_t$`、`$\lambda_w=0.5$`。行内公式中禁止出现 `\begin{...}`、`\tag{...}`、换行、长损失函数或多项对齐。
3. 所有长公式、多行公式、带编号公式必须使用 GitHub 官方推荐的 fenced math block，禁止使用 `$$...$$`：

````markdown
```math
\mathcal{L}_\mathrm{act}
= \mathbb{E}_{\tau,\boldsymbol{\epsilon}}
\left\lVert
\mathbf{v}_\xi(\mathbf{A}_t^\tau,\tau,\mathbf{H}_\mathrm{ACT})
-
(\mathbf{A}_t-\boldsymbol{\epsilon})
\right\rVert_2^2
\tag{1}
```
````

4. 如果需要公式编号，`\tag{...}` 必须放在同一个 `math` 代码块内部的最后一行附近，禁止把 `\tag{...}` 放到普通正文行。
5. 避免 GitHub MathJax 容易不兼容的宏：不要使用 `\textsc{...}`，改用 `\mathrm{...}` 或 `\operatorname{...}`；范数使用 `\left\lVert ... \right\rVert`；拼接符用 `\Vert` 或 `\mid`，不要写成复杂的 delimiter hack。
6. 如果原文公式太复杂，优先拆成多个较短的 `math` 代码块，并在公式后用中文解释变量含义；不要输出无法确定能渲染的 LaTeX。
7. 输出结束前自检：全文不得出现 `$$`；不得出现同一行内同时包含标题和正文；所有 `\begin` / `\tag` 必须只出现在 `math` fenced block 内。

固定结构如下：

# <论文标题>

## 基本信息

- arXiv
- Authors
- Categories
- 一句话结论

## 研究问题

说明论文要解决的核心科学/工程问题，以及它与 VLA、World Model、Embodied AI 或机器人控制的关系。

## 任务与挑战

解释具体任务、输入输出、训练/评测设定，以及为什么已有方法不够好。

## 核心 Insight

用 1-3 段讲清本文最核心的思想。若有总览图或架构图，在这里插入。

## 方法与公式

解释模型结构、数据流、训练目标、推理流程和关键公式。公式不要只贴出来，要逐项解释变量含义和算法作用。

## 贡献拆解

列出 2-4 个真正的技术贡献，每个贡献包含“做了什么”“为什么有效”“和已有方法差别在哪里”。

## 关键图表解读

对每张精选图给出独立解读：图展示什么、支撑哪条论点、读图时要注意什么。

## 实验与消融

总结数据集、基线、指标、主结果、消融实验和最能支撑论文结论的数据。必须指出最强证据和最存疑证据。

## 局限性

客观指出实验设置、泛化性、可复现性、算力/数据依赖或理论论证上的不足。

## 个人研究判断

面向“World Models assisting Embodied AI downstream tasks”的研究方向，判断这篇论文是否值得继续追踪，并给出理由。
